# ---- LINTING & QUALIDADE DE CÓDIGO ----
ruff==0.15.13               # Linter e formatador de código extremamente rápido (substitui Flake8/Isort)
interrogate==1.7.0          # Verifica a cobertura de docstrings no projeto
pre-commit==4.6.0           # Gerencia hooks para rodar verificações antes de cada commit
vulture==2.16               # Detecta código Python não utilizado (dead code)
pylint==3.0.0               # Utilizado especificamente para analisar código duplicado

# ---- SEGURANÇA ----
bandit==1.9.4               # Analisa o código em busca de vulnerabilidades comuns de segurança
pip-audit==2.10.0           # Verifica dependências em busca de vulnerabilidades conhecidas (CVEs)

# ---- TESTES ----
pytest==9.0.3               # Framework principal para escrita e execução de testes
pytest-asyncio==1.3.0       # Extensão para suporte a testes de funções assíncronas (async/await)
pytest-mock==3.15.1         # Facilita o uso de objetos Mock e Spies dentro do Pytest
pytest-timeout==2.4.0       # Interrompe testes que travam além do limite configurado
coverage==7.14.0            # Mede a porcentagem de linhas de código cobertas pelos testes
lightgbm==4.6.0             # Gradient boosting tabular para meta-classificador offline
optuna==4.2.1               # Otimizacao Bayesiana de hiperparametros do meta-classificador
scikit-learn==1.6.1         # Metricas e utilitarios de treino tabular
joblib==1.4.2               # Serializacao de artefatos .pkl do meta-classificador

