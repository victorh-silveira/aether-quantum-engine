# ---- COMUNICACAO & REDE ----
websockets==16.0               # Comunicação bidirecional em tempo real (WebSockets assíncrono)
httpx==0.28.1                  # Cliente HTTP moderno com suporte a requisições assíncronas (async/await)

# ---- AMBIENTE & CONFIGURACAO ----
python-dotenv==1.2.2           # Carrega variáveis de ambiente a partir de um arquivo .env

# ---- PROCESSAMENTO DE DADOS ----
numpy==2.4.6                   # Computação numérica de alta performance e manipulação de arrays multidimensionais
pandas==2.3.3                  # DataFrames meta tabular / LightGBM / Docker meta (nao misturar com polars no mesmo modulo)
polars==1.23.0                 # DataFrames features DL runtime (TCN); dual-stack SSOT com pandas — sem 3a lib DF

# ---- INTERFACE & UI ----
rich==15.0.0                   # Renderização de texto formatado, cores, tabelas e logs bonitos no terminal

# ---- INFRAESTRUTURA ----
redis[hiredis]>=5.0            # Banco de dados em memória para cache/mensageria + parser ultrarrápido em C (hiredis)
asyncpg>=0.30                  # Driver de conexão assíncrono de alta performance para PostgreSQL
minio>=7.2                     # SDK para integração com armazenamento de objetos compatível com AWS S3

# ---- DEEP LEARNING & INFERENCIA ----
torch==2.10.0                  # Framework principal de Deep Learning (PyTorch) para criação e execução de redes neurais

# ---- TREINAMENTO OFFLINE (meta-classificador tabular) ----
lightgbm==4.6.0                # Framework de Gradient Boosting baseado em árvores de decisão para alta performance em dados tabulares
optuna==4.2.1                  # Framework de otimização de hiperparâmetros automatizada (Hyperparameter Tuning)
scikit-learn==1.6.1            # Biblioteca clássica de Machine Learning para pré-processamento, métricas e modelos básicos
joblib==1.4.2                  # Serializacao explicita meta/loss (import direto; nao depender so do transitivo sklearn)
