---
description: Gates de execucao Aether — tecnico + HARD SKIP por P_LOSS
globs: app/src/application/services/execution_*.py
alwaysApply: true
---

# Execution gates

- SKIP tecnico: `training` / `data` / `deploy` / `predict_error` / stop-win `EXEC_PAUSE`
- Anti-loss unico vivo: loss-classifier HARD SKIP se `p_loss >= hard_p_loss_floor` (**0.90**), `gate_reason=loss_clf` — **sem FLIP**, **sem Soft Kelly** do loss-clf
- TCN escolhe CALL/PUT; Kelly + SIDE_EQ sizing; SCALE vision so telemetria
- **Proibido** reabrir quality gate amplo, signal_skip multi-gate, fusao EV, micro/regime/vol/exhaust/neg_edge, anti-loss EMA/RSI
- Catalogo: `docs/engineering-indicator-gates.md` + playbook
