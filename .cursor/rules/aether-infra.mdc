---
description: Infra Aether — Redis, Timescale, MinIO, meta, loss, state stores
globs: app/src/infrastructure/state/**/*.py,app/src/infrastructure/storage/**/*.py,app/src/infrastructure/market/**/*.py,app/src/infrastructure/inference/**/*.py,app/src/infrastructure/factories/**/*.py,infra/docker/**
alwaysApply: true
---

# Infra

- Motor no host; Redis/Timescale/MinIO/meta/loss via Docker em `127.0.0.1`
- Profiles: `core` / `ml` — ver `docs/infra-docker.md`
- Targets: `make docker-up`, `docker-up-core`
- Inferencia TCN: eager/CUDA local no host (`data/dl`)
- Timeouts Redis: nao “consertar” lentidao removendo resiliencia
- State: Redis + JSON; settlement em Redis ZSET
- MinIO: artefactos/checkpoints; sanity TorchScript no host quando aplicavel
- Skill: `aether-infra-stack`
