---
description: Doutrina LLM Aether — TCN + FLIP P_LOSS
alwaysApply: true
---

# Doutrina LLM Aether

LLM/Cursor e copiloto de engenharia. Nao decide CALL/PUT em runtime (TCN + loss-clf FLIP no piso + Kelly). Doutrina: `docs/llm-trading-doctrine.md`. Arquitetura: `docs/engineering-architecture-senior.md`.

Runtime SSOT: **1HZ75V** M5; micro/MINI **300 s**; macro **86400 s**; lookback **30**; ciclo **300 s**; contrato **5 m**; payout **0.85**; `online_training` **false**; `invert_exec_side` **false**; `skip_neg_edge` **true**; `skip_exec_vs_candle` / `skip_scale_candle_discord` / `skip_doji` **false**; `skip_below_soft_min_acc` **false**; `adapt_retract_enabled` **false**; cover amort **1/1**; stop-win **4.31%** Single-Strike. Motor host Python 3.13 + asyncio; Docker `core,ml`.

## Nunca propor

- `force_trade_every_cycle=true` como “fix” de EXEC_EMPTY
- Reabrir SCALE adapt / skips candle/doji / Soft Kelly META ou loss-clf / quality gate amplo / HARD SKIP / trava `cal_soft_edge`
- Inflar stake apos LOSS (revenge) ou “operar mais para aprender” com N baixo
- Remover caps (`max_safe_stake_*`), settlement Redis ou timeouts “temporariamente”
- Afrouxar `hard_p_loss_floor` / `veto_mode=hard` sem mandato e evidencia

## Sempre fazer

- Distinguir EXPLORE vs RECOVER; `cover_enabled` **true** (`cover_multiple` **1.0**, amort **1/1**, stake = `min(max(PEND/payout, 1% banca), cap_L0)`); cover L0 **3.5%**; PEND material nao force-explore por near-stop; piso Kelly **1%** soberano; Single-Strike so ALLOW
- Direcao = **TCN + FLIP** se `p_eff` >= **0.58** young / **0.58** mature; so apos auto_learn (`LOSS_BOOTSTRAP_EXIT_N` **4**) e `n_train >= flip_min_n_train` **4**; sem `candle_holds`
- SKIP `neg_edge` em EXPLORE; **waive** com PEND material; META nao comprime stake
- Tratar EXEC_EMPTY tecnico / `neg_edge` como sucesso de processo quando coerente (EXPLORE); Cal~0.52 + Edge≤0 = `neg_edge` correto ate **retreino + export** (`force_ok`)
- Exigir evidencia: `live_n`, Cal/Edge (EV vs BE), linha LOSS_CLF (`p=` / `pe=` / `floor=` / `boot=N/4` / `n=`)

## Diagnostico de log

Ordem: CLUSTER → `[GATES] || LOSS_CLF` → KELLY/cover_l0 → EXEC → RESOLVED.
`blocked=bootstrap boot=N/4` = FLIP off; `blocked=flip_min_n` = `n_train < 8`. Edge = EV; **Edge ≤ 0 = SKIP `neg_edge` em EXPLORE**; com PEND material o SKIP e waived. Cover amort=1 → `recovery_cap_mode=cover_l0`. `LIN:` no RESOLVED e pos-settle.
Proibido narrar “mao quente”.
