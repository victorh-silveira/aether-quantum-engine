---
description: Dependencias Python — anti-redundancia, dual-stack pandas/polars, ABI numpy
globs: "**/requirements*.txt"
alwaysApply: false
---

# Python deps

- Declarar o que o codigo importa; nao pinar so-transitivo (`coverage` sob `pytest-cov`)
- Dual-stack SSOT: `polars` = features DL; `pandas` = meta tabular; sem misturar no mesmo modulo; sem 3a lib DF
- Manter `joblib` pin se houver `import joblib` (meta/loss)
- Bump `numpy`/`torch`/`sklearn` exige `pip check` + smoke import no WSL
- Doc: `docs/engineering-python-deps.md`; skill: `aether-python-deps`
