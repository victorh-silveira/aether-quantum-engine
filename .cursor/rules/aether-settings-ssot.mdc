---
description: Settings SSOT Aether — knobs fail-closed e parsers
globs: config/settings.json,**/config_knobs.py,**/execution_quality_gate_config.py,**/kelly_runtime_config.py,**/soft_recovery_config.py
alwaysApply: true
---

# Settings SSOT

- Knob novo = settings + resolve_* + teste + doc se mudar semantica
- Nao hardcodar o mesmo default em tres lugares
- Nao afrouxar: force_trade, min_payoff_edge, ACC gate, max_safe_stake_*, sample_size_policy (`evidence_n_min`/**12**, `large_n_min`/**32**)
- Proteger doutrina de lado/sizing: `infra.loss_classifier.flip_*` (`flip_require_auto_learn`, `flip_seed_*`, `flip_block_when_tcn_pos_edge`, `flip_waive_tcn_pos_edge_on_discord`), `orchestrator.execution.scale_vision.fusion_*` (`fusion_block_when_tcn_pos_edge`, soft **0.50/0.25**, `fusion_meta_ev_weight` **0.0**, `fusion_loss_weight` **0.0**, `fusion_loss_seed_weight_mult` **0.0**), `scale_vision.ops_window_bars` **3**, `signal_skip.neg_edge_*` (`neg_edge_hard_skip` **true** → Edge<=0 HARD; Edge < `min_edge_*` (**0.015** explore = recovery) HARD `neg_edge_subfloor_hard`; Soft_SIZE so soft flags com Edge >= floor; Single-Strike so ALLOW; `fusion_p_eff` nao lava hard), `anti_loss_hard_skip` / `anti_loss_min_candle_body` (**0.10**) / `anti_loss_live_weak_candle_enabled` (**true**) / `anti_loss_live_confirm_enabled` (**true**) / `anti_loss_live_confirm_min_body` (**0.15** no corpo liquido da janela ops, ancora **EXEC**) / `anti_loss_live_exec_candle_enabled` (**false**) / `anti_loss_allow_candle_flip` (**true** → flip microestrutura **so se** Edge Cal vela >= floor **0.015**; hybrid disagree → `live_discord_weak`) / RSI **0.30/0.70**, `invert_exec_side`, `online_training`, `cover_enabled` **false**, piso Kelly **1%**, Soft_SIZE **2.5%** so se Edge>=**0.015**, caps recovery, `require_signature_boundary` **true**, ciclo **300 s**
- Auth/PAT: nao commitar segredos
- Doc: `docs/engineering-settings-ssot.md`; skill: `aether-settings-change`
