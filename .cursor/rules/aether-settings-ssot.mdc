---
description: Settings SSOT Aether — knobs fail-closed e parsers
globs: config/settings.json,**/config_knobs.py,**/execution_quality_gate_config.py,**/kelly_runtime_config.py,**/soft_recovery_config.py
alwaysApply: true
---

# Settings SSOT

- Knob novo = settings + resolve_* + teste + doc se mudar semantica
- Nao hardcodar o mesmo default em tres lugares
- Nao afrouxar: force_trade, min_payoff_edge, ACC gate, max_safe_stake_*, sample_size_policy (`evidence_n_min`/**12**, `large_n_min`/**32**)
- Proteger doutrina de lado/sizing: `infra.loss_classifier.flip_*` (`flip_require_auto_learn`, `flip_allow_seed_on_candle_discord` **true**, `flip_waive_guards_above_p_loss` **0.85**, `flip_seed_*`, `flip_block_when_tcn_pos_edge`, `flip_waive_tcn_pos_edge_on_discord`), `orchestrator.execution.scale_vision.fusion_*` (`fusion_block_when_tcn_pos_edge`, soft **0.50/0.25**, `fusion_meta_ev_weight` **0.0**, `fusion_loss_weight` **0.0**, `fusion_loss_seed_weight_mult` **0.0**), `scale_vision.ops_window_bars` **3**, `signal_skip.neg_edge_*` (`neg_edge_hard_skip` **true** → Edge<=0 HARD; Edge < `min_edge_*` (**0.015** explore = recovery) HARD `neg_edge_subfloor_hard`; Soft_SIZE so soft flags com Edge >= floor; excecao `loss_clf_flip` → Soft neg_edge; Single-Strike so ALLOW; `fusion_p_eff` nao lava hard), `anti_loss_hard_skip` / `anti_loss_min_candle_body` (**0.10**) / `anti_loss_live_weak_candle_enabled` (**false**) / `anti_loss_live_confirm_enabled` (**false**) / `anti_loss_allow_candle_flip` (**false**) / `anti_loss_seed_discord_enabled` (**false**) / `regime_gate_enabled` (**true**) / `regime_adx_max` / `regime_bb_squeeze_enabled` (**true**), `micro_discord_follow_candle` (**true**) / `micro_discord_follow_kelly_mult` (**0.55**) (FOLLOW Soft_SIZE so se Edge Cal da vela >= **0.015**; senao HARD `micro_discord`; skip se `loss_clf_flip`), `invert_exec_side`, `online_training`, `cover_enabled` **false**, piso Kelly **1%**, Soft_SIZE **2.5%** so se Edge>=**0.015**, caps recovery, `require_signature_boundary` **true**, ciclo **300 s**
- Auth/PAT: nao commitar segredos
- Doc: `docs/engineering-settings-ssot.md`; skill: `aether-settings-change`

- `regime_gate_enabled` **true** + ADX/squeeze -> HARD `regime_squeeze` (sem flip direcional); TCN 14D / meta 23D; call/put **0.53/0.47**; `ema_50` **50**.
