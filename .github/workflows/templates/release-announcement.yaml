# Aether Quantum Engine — Release {VERSION}

CI gerou uma nova versao do motor **Deriv Rise/Fall** (TCN + meta + gates).

### Detalhes
- **Versao:** `{VERSION}`
- **Data:** `{DATE}`
- **Config:** `config/settings.json` (`deep_learning`, `risk_management`, `orchestrator`)

### Historico
{CHANGELOG}

---
Release automatico (semantic-release).
